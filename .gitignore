# Obsidian per-device UI state — changes constantly on every machine,
# causes sync conflicts between devices. Never worth versioning.
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# Python bytecode — never worth versioning
__pycache__/
*.pyc
